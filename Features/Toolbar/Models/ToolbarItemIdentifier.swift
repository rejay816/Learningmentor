import AppKit

enum ToolbarItemIdentifier {
    static let newChat = NSToolbarItem.Identifier("newChat")
    
    // 为未来可能添加的其他工具栏项预留空间
    // static let otherItem = NSToolbarItem.Identifier("otherItem")
} 